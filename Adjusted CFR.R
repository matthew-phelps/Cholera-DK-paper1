# Adjusted CFR after accounting for underrreporting
rm(list = ls())


sum(cph_counts_age$total_dead) / (sum(cph_pop1853_10yr$total1853) * .58)
sum(cph_counts_age$total_dead) / (sum(cph_pop1853_10yr$total1853) * .69)
